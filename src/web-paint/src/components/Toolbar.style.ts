import * as React from "react";

interface IToolbarStyle {
  buttonGroup: React.CSSProperties;
}

const style: IToolbarStyle = {
  buttonGroup: {
    marginLeft: "8px",
    marginRight: "8px",
  },
};

export default style;
